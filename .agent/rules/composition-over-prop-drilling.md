---
trigger: always_on
---

Avoid passing props through multiple layers. Use component composition or the Context API for state sharing, and use the "Compound Component" pattern for complex UI elements to keep interfaces clean.